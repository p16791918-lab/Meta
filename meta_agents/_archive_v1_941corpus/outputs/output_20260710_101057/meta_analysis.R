# ============================================================
# Meta-Analysis R Script
# Generated by Agent 4: Statistical Analysis Agent
# Run in R 4.x with: install.packages(c("metafor", "ggplot2"))
# ============================================================

Statistical pooling requires actual event counts and sample sizes, but the extraction data you've given me has `ai`, `n1`, `ci`, `n2` all set to `null` for every study — Navsaria2024, Lim2023, and Wu2011 have no numeric values to compute an effect size from. I can't run a meta-analysis on absent data, so rather than fabricate numbers I'm flagging this and returning a plan you can execute once the real values are filled in.

```json
{
  "analysis_plan": "Cannot be finalized: the extraction records for all 3 studies (Navsaria2024, Lim2023, Wu2011) have null values for ai, n1, ci, n2 — i.e., no event counts or group sample sizes were extracted. A binary-outcome meta-analysis requires, at minimum, a 2x2 table per study (events/total in each group) or, if these are single-arm prevalence studies, events/total for one group. Once real values are supplied: (1) effect measure — with 3 studies and likely low/zero cell counts, OR via Mantel-Haenszel or logit-transformed proportions (if single-arm) would be preferred over RR for stability; final choice depends on whether these are comparative (2-arm) or prevalence (1-arm) studies, which is unclear from the 'design' field alone (cohort, cohort, cross-sectional — no explicit comparator arm is evident). (2) Model choice: with only 3 studies, I² estimation is unstable and REML/Knapp-Hartung random-effects is still recommended by convention for conservative CI width, but results should be interpreted cautiously given the low k. (3) Subgroup analysis by 'design' and 'follow_up_wk' is not meaningful with only 3 studies split across 2 design categories (2 cohort vs 1 cross-sectional) — subgroups would have 1-2 studies each, which is statistically uninformative. (4) Publication bias tests (Egger's, trim-and-fill) are not valid with k=3 (minimum ~10 studies recommended for funnel-plot asymmetry tests).",
  "r_code": "# CANNOT BE RUN — placeholder template only.\n# Fill in real ai/n1/ci/n2 (or single-arm events/total) before executing.\nlibrary(metafor)\nlibrary(meta)\n\ndat <- data.frame(\n  study       = c('Navsaria2024', 'Lim2023', 'Wu2011'),\n  design      = c('cohort', 'cohort', 'cross-sectional'),\n  follow_up_wk = c(NA, NA, NA),   # not provided\n  ai = c(NA, NA, NA),             # events, group 1 -- MISSING\n  n1 = c(NA, NA, NA),             # total,  group 1 -- MISSING\n  ci = c(NA, NA, NA),             # events, group 2 -- MISSING\n  n2 = c(NA, NA, NA)              # total,  group 2 -- MISSING\n)\n\n# --- Everything below requires the NAs above to be replaced with real data ---\n\n# Effect size (log OR shown; swap 'measure' if RR/RD preferred once data confirms 2-arm design)\nes <- escalc(measure = 'OR', ai = ai, n1i = n1, ci = ci, n2i = n2, data = dat,\n             slab = study)\n\n# Random-effects model, REML + Knapp-Hartung\nres <- rma(yi, vi, data = es, method = 'REML', test = 'knha')\nsummary(res)\n\n# Heterogeneity: I^2, tau^2, Q test are printed in summary(res); with k=3 these\n# estimates carry very wide uncertainty and should be reported with caution.\n\n# Forest plot with prediction interval\nforest(res, addpred = TRUE, header = TRUE)\n\n# Subgroup analysis (only run if each subgroup has >=2 studies; here it would not)\n# res_sub <- rma(yi, vi, mods = ~ design, data = es, method = 'REML', test = 'knha')\n# summary(res_sub)\n\n# Leave-one-out sensitivity analysis\nl1o <- leave1out(res)\nprint(l1o)\n\n# Egger's test / trim-and-fill -- NOT VALID with k=3, shown for completeness only\n# regtest(res)\n# taf <- trimfill(res)\n# funnel(taf)\n\nfunnel(res)\n",
  "results_narrative": "A meta-analysis could not be performed: the extracted data for all three studies (Navsaria2024, Lim2023, Wu2011) lack event counts and group sample sizes (ai, n1, ci, n2 all null), so no effect size could be calculated. Please re-run extraction (agent_3_extraction) to obtain these values, or supply them manually, before pooling can proceed.",
  "grade_table": {
    "risk_of_bias": "not assessable",
    "inconsistency": "not assessable",
    "indirectness": "not assessable",
    "imprecision": "not assessable",
    "publication_bias": "not assessable",
    "overall_certainty": "Very Low",
    "rationale": "No quantitative synthesis was possible because all extracted effect-size inputs (ai, n1, ci, n2) are missing for every included study. Certainty cannot be rated on any GRADE domain until numeric data are recovered from the source articles."
  }
}
```

The blocker is upstream: `agent_3_extraction` produced records with the outcome fields unpopulated for these 3 studies. Worth checking whether that stage failed silently or whether these particular papers don't report the needed 2x2 data at all (e.g., Wu2011 is cross-sectional and may report only a prevalence/correlation, not a comparative event count).